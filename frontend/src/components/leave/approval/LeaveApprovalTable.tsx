"use client";

import { CheckIcon, ChevronDoubleLeftIcon, ChevronDoubleRightIcon, ChevronLeftIcon, ChevronRightIcon } from "@heroicons/react/24/outline";
import type { LeaveApprovalRow } from "./leaveApprovalTypes";
import { STATUS_LABELS } from "./leaveApprovalTypes";

const statusStyles = { PENDING: "bg-amber-50 text-amber-700", APPROVED: "bg-emerald-50 text-emerald-700", REJECTED: "bg-rose-50 text-rose-700" };
function date(value: string) { return value.slice(0, 10).replaceAll("-", "."); }
function leaveTypeStyle(name: string) { if (name.includes("연차")) return "bg-indigo-50 text-indigo-700"; if (name.includes("반차")) return "bg-sky-50 text-sky-700"; if (name.includes("경조")) return "bg-emerald-50 text-emerald-700"; if (name.includes("병가")) return "bg-amber-50 text-amber-700"; return "bg-gray-100 text-gray-600"; }
interface Props { rows: LeaveApprovalRow[]; total: number; loading: boolean; selectedIds: number[]; page: number; pageSize: number; onPage: (page: number) => void; onPageSize: (size: number) => void; onToggle: (id: number) => void; onToggleAll: () => void; onApprove: (row: LeaveApprovalRow) => void; onReject: (row: LeaveApprovalRow) => void; onDetail: (row: LeaveApprovalRow) => void; onBulk: () => void; }

export default function LeaveApprovalTable(props: Props) {
  const { rows, total, loading, selectedIds, page, pageSize } = props; const totalPages = Math.max(1, Math.ceil(total / pageSize)); const pending = rows.filter((row) => row.status === "PENDING"); const allSelected = pending.length > 0 && pending.every((row) => selectedIds.includes(row.leaveRequestId));
  return <section id="leave-request-list" className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-gray-100 p-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-2"><h2 className="text-lg font-bold text-gray-900">신청 목록</h2><span className="rounded-full bg-gray-100 px-2.5 py-1 text-xs font-bold text-gray-600">총 {total}건</span></div><div className="flex items-center gap-2"><button type="button" onClick={props.onBulk} disabled={selectedIds.length === 0} className="flex h-9 items-center gap-2 rounded-lg border border-emerald-200 bg-emerald-50 px-3 text-sm font-semibold text-emerald-700 hover:bg-emerald-100 disabled:opacity-40"><CheckIcon className="h-4 w-4" />{selectedIds.length ? `선택 ${selectedIds.length}건 승인` : "선택 일괄승인"}</button><select value={pageSize} onChange={(event) => props.onPageSize(Number(event.target.value))} className="h-9 rounded-lg border border-gray-200 bg-white px-3 text-sm text-gray-600"><option value={10}>10개씩 보기</option><option value={20}>20개씩 보기</option><option value={50}>50개씩 보기</option></select></div></div><div className="overflow-x-auto"><table className="w-full min-w-[1180px] text-left text-sm"><thead className="bg-gray-50 text-xs text-gray-500"><tr><th className="w-12 px-5 py-3"><input type="checkbox" checked={allSelected} onChange={props.onToggleAll} aria-label="승인 대기 전체 선택" /></th>{["신청일","이름","부서","직급","휴가 유형","휴가 기간","일수","사유","상태","관리"].map((label) => <th key={label} className="px-4 py-3 font-semibold">{label}</th>)}</tr></thead><tbody>{loading ? <tr><td colSpan={11} className="py-16 text-center text-gray-500">휴가 신청 정보를 불러오는 중입니다.</td></tr> : rows.length === 0 ? <tr><td colSpan={11} className="py-16 text-center text-gray-500">조건에 맞는 휴가 신청이 없습니다.</td></tr> : rows.map((row) => <tr key={row.leaveRequestId} className="border-t border-gray-100 hover:bg-gray-50/60"><td className="px-5 py-4">{row.status === "PENDING" && <input type="checkbox" checked={selectedIds.includes(row.leaveRequestId)} onChange={() => props.onToggle(row.leaveRequestId)} aria-label={`${row.employeeName} 선택`} />}</td><td className="whitespace-nowrap px-4 py-4 text-gray-600">{date(row.createdAt)}</td><td className="px-4 py-4"><button type="button" onClick={() => props.onDetail(row)} className="flex items-center gap-3 text-left"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-indigo-100 text-xs font-bold text-indigo-700">{row.employeeName.charAt(0)}</span><span><strong className="block text-gray-900">{row.employeeName}</strong>{row.employeeNo && <small className="text-gray-400">{row.employeeNo}</small>}</span></button></td><td className="px-4 py-4 text-gray-600">{row.departmentName || "-"}</td><td className="px-4 py-4 text-gray-600">{row.positionName || "-"}</td><td className="px-4 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${leaveTypeStyle(row.leaveTypeName)}`}>{row.leaveTypeName}</span></td><td className="whitespace-nowrap px-4 py-4 text-gray-600">{row.startDate === row.endDate ? date(row.startDate) : `${date(row.startDate)} ~ ${date(row.endDate)}`}</td><td className="px-4 py-4 font-semibold text-gray-800">{row.leaveDays}일</td><td className="max-w-36 truncate px-4 py-4 text-gray-500" title={row.reason || ""}>{row.reason || "-"}</td><td className="px-4 py-4"><span className={`whitespace-nowrap rounded-full px-2.5 py-1 text-xs font-semibold ${statusStyles[row.status]}`}>{STATUS_LABELS[row.status]}</span></td><td className="px-4 py-4"><div className="flex gap-2">{row.status === "PENDING" ? <><button type="button" onClick={() => props.onApprove(row)} className="rounded-md bg-[#4A5DDF] px-3 py-1.5 text-xs font-semibold text-white hover:bg-blue-700">승인</button><button type="button" onClick={() => props.onReject(row)} className="rounded-md border border-rose-300 px-3 py-1.5 text-xs font-semibold text-rose-600 hover:bg-rose-50">반려</button></> : <button type="button" onClick={() => props.onDetail(row)} className="rounded-md border border-gray-200 px-3 py-1.5 text-xs font-medium text-gray-600 hover:bg-gray-50">상세</button>}</div></td></tr>)}</tbody></table></div><div className="flex items-center justify-between border-t border-gray-100 p-4"><span className="text-sm text-gray-500">총 {total}건</span><div className="flex gap-1"><PageButton label="처음" disabled={page === 0} onClick={() => props.onPage(0)} icon={<ChevronDoubleLeftIcon className="h-4 w-4" />} /><PageButton label="이전" disabled={page === 0} onClick={() => props.onPage(page - 1)} icon={<ChevronLeftIcon className="h-4 w-4" />} />{Array.from({ length: totalPages }, (_, index) => index).map((index) => <button type="button" key={index} onClick={() => props.onPage(index)} className={`h-8 min-w-8 rounded-md px-2 text-sm font-medium ${page === index ? "bg-indigo-600 text-white" : "border border-gray-200 text-gray-600 hover:bg-gray-50"}`}>{index + 1}</button>)}<PageButton label="다음" disabled={page >= totalPages - 1} onClick={() => props.onPage(page + 1)} icon={<ChevronRightIcon className="h-4 w-4" />} /><PageButton label="마지막" disabled={page >= totalPages - 1} onClick={() => props.onPage(totalPages - 1)} icon={<ChevronDoubleRightIcon className="h-4 w-4" />} /></div></div></section>;
}
function PageButton({ label, disabled, onClick, icon }: { label: string; disabled: boolean; onClick: () => void; icon: React.ReactNode }) { return <button type="button" aria-label={label} disabled={disabled} onClick={onClick} className="flex h-8 w-8 items-center justify-center rounded-md border border-gray-200 text-gray-500 hover:bg-gray-50 disabled:opacity-40">{icon}</button>; }
